import { createContext } from "react";

export const scrolPause = createContext(false)
export const Coordinates = createContext({})
export const Address = createContext()
export const PlaceName = createContext()
export const Locality = createContext()
export const CartContext = createContext([])